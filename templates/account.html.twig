{% extends 'base.html.twig' %}

{% block body %}

    <div>
        {{ form_start(formInfo) }}
        {{ form_row(formInfo.email) }}
        {{ form_errors(formInfo.email) }}
        {{ form_row(formInfo.password) }}
        {{ form_errors(formInfo.password) }}
        {{ form_end(formInfo) }}
    </div>


{% endblock %}